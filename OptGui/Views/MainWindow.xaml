<Window x:Class="OptGui.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OptGui"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1">
            <ListView
                ItemsSource="{Binding ProblemType1918Names}"
                SelectedItem="{Binding SelectedRowProblemType1918}"
            />
        </StackPanel>
        <StackPanel Grid.Row="2">
            <Button Width="80" Height="40" Command="{Binding CommandLoad}" Content="LOAD"/>
        </StackPanel>
    </Grid>
</Window>
